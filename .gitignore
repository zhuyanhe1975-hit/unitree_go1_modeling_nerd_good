__pycache__/
*.pyc

# Runtime outputs
runs/*.png
runs/*.pt
runs/*.npz
runs/mjcf_patched.xml
runs/warp_cache/
runs_smoke/
runs_*/

# Local real logs / captures (do not push by default)
real_data/*.csv
!real_data/README.md

# Generated results (large artifacts)
results/*
!results/README.md

# Keep the Unitree SDK build artifacts out of git (rebuildable)
runs/unitree_sdk_build/

# Legacy preview generated under sudo can become root-owned
real_data_preview.png
